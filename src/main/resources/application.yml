server:
  shutdown: graceful
  port: 3000
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
api:
  security:
    token:
      secret: ${JWT_SECRET:9312938sz8casmd189238}